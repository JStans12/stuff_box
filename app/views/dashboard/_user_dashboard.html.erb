<div class= "col-md-3">
  <h4>Folders</h4>
  <% current_folder.children.each do |folder| %>
    <h4><%= link_to folder.name, current_folder_path(folder.id) %></h4>
    <% end %>
    <br>
    <%= link_to "Parent Folder", up_path %>
    <br>
    <%= link_to "Root", root_folder_path %>
</div>
